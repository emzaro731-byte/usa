const menu=document.querySelector('.menu');const nav=document.querySelector('#navLinks');const form=document.querySelector('#contactForm');const msg=document.querySelector('#formMessage');
menu?.addEventListener('click',()=>{nav.style.display=nav.style.display==='flex'?'none':'flex'});
document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>{if(innerWidth<=800)nav.style.display='none'}));
document.querySelector('#year').textContent=new Date().getFullYear();
form?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=(data.get('name')||'').trim();const email=(data.get('email')||'').trim();const phone=(data.get('phone')||'').trim();const service=data.get('service')||'Other';const message=(data.get('message')||'').trim();if(!name||!email||!message){msg.textContent='Please complete your name, email and message.';msg.className='form-message error';return;}const text=`Hello Business With The USA, I would like to make a business enquiry.%0A%0AName: ${encodeURIComponent(name)}%0AEmail: ${encodeURIComponent(email)}%0APhone: ${encodeURIComponent(phone||'Not provided')}%0AService: ${encodeURIComponent(service)}%0A%0AMessage:%0A${encodeURIComponent(message)}`;msg.textContent='Opening WhatsApp with your enquiry...';msg.className='form-message success';window.open(`https://wa.me/17868769555?text=${text}`,'_blank','noopener');form.reset();});